import React from "react";
// import Poem from "./Poem";

const FavoritePoem = (props) => {
  return (
    <div className="favorite-container">
    {console.log(props)}
    {/* {props.favorite()} */}
      {/* {props.favorites.map(poem => <Poem key={poem.id} {...poem} }/>)} */}
    </div>
  );
}


export default FavoritePoem;
